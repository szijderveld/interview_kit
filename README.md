# interviewer

A Python package that conducts adaptive voice interviews on behalf of a human operator. The operator defines a Conversation (persona, purpose, goals); the engine runs it as a voice agent, adapts mid-call, and produces a structured Extract. See [SCOPE.md](SCOPE.md) for the full design contract.
