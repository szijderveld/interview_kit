"""LLM client implementations of the :class:`LLMClient` protocol.

Default impl is :class:`AnthropicLLMClient` (D2). The submodules are
internal building blocks; consumers should import the client class via
``from interviewer.llm.anthropic import AnthropicLLMClient``.
"""
