"""LiveKit AgentSession integration.

Importing from this package requires the ``voice`` install extras
(``pip install interview-kit[voice]``) which pull in ``livekit-agents``
and ``livekit-api``. Simulator-only consumers never need to import it.
"""
